{
  "name": "bet-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}