{
  "name": "bet-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install --include=dev && npx tsc && cp knexfile.js dist/ && cp -r migrations dist/",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}